import { Text } from "react-native-paper";
import { ScrollView } from "react-native-web";

const Settings = () => {
  return (
    <ScrollView>
      <Text>Test</Text>
    </ScrollView>
  )
}

export default Settings;